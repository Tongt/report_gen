# 五看五定智能体使用说明（小白版）

## 第一步：安装 Python

1. 电脑里先安装 Python 3.9 及以上版本。  
2. 安装时请勾选“Add Python to PATH”（Windows）。  

---

## 第二步：解压工具包

1. 把整个 `wukan-wuding-agent` 文件夹放到本地任意目录。  
2. 不要改动子目录结构。  

---

## 第三步：双击第一次安装

- Mac：双击 `Mac第一次安装.command`  
- Windows：双击 `Windows第一次安装.bat`  

该步骤会自动：
- 创建虚拟环境 `.venv`
- 安装依赖

---

## 第四步：双击启动

- Mac：双击 `Mac启动.command`  
- Windows：双击 `Windows启动.bat`  

启动后浏览器会打开：

`http://localhost:8501`

---

## 第五步：网页填写 API Key

首次进入页面会看到“API Key 配置区”，按提示：
1. 输入 DashScope API Key（密码框显示）
2. 点击“测试 API Key 是否可用”
3. 点击“保存 API Key”

保存后会自动写入 `config/user_settings.json`，下次启动无需重复填写。

---

## 第六步：上传资料并生成报告

1. 上传资料（`pdf/docx/md`）。  
2. 输入分析对象。  
3. 点击生成报告。  

---

## 文件保存位置说明

- 上传资料保存到：`knowledge_base/raw_uploads/`
- 报告保存到：`outputs/`
