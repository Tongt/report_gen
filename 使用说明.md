# 五看五定智能体使用说明

## 方式一：在线（Streamlit Community Cloud）

1. 打开部署后的应用链接。
2. **API Key**：在页面按提示填写**您自己的** DashScope Key 并保存；侧栏「API Key」可随时更换。勿依赖部署方在 Secrets 里配置的共享 Key（本项目也不会从 Streamlit Secrets 读取 `DASHSCOPE_API_KEY`）。
3. 上传资料、输入分析对象、生成报告。

说明：免费云实例存储多为临时性，重启后此前上传的资料与向量索引可能需重新上传或重建。

---

## 方式二：本地运行

### 1. 安装 Python

安装 **Python 3.9+**（建议 **3.11**，与线上一致）。Windows 安装时请勾选将 Python 加入 PATH。

### 2. 准备项目

将 `wukan-wuding-agent` 文件夹放到本地任意目录，尽量保持子目录结构不变。

### 3. 创建环境并安装依赖

在终端进入项目目录后执行：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

Windows：`\.venv\Scripts\activate` 后再 `pip install -r requirements.txt`。

### 4. 启动

```bash
streamlit run app.py
```

浏览器访问终端里给出的地址（一般为 `http://localhost:8501`）。

### 5. API Key（本地）

首次进入若未配置 Key，页面会出现配置区：

1. 输入 DashScope API Key  
2. 可先点「测试 API Key 是否可用」  
3. 再点「保存 API Key」  

保存后写入 `config/user_settings.json`，下次启动自动读取。也可使用项目根目录 `.env` 中的 `DASHSCOPE_API_KEY`（见 `.env.example`）。

### 6. 使用应用

1. 上传资料（`pdf` / `docx` / `md`）  
2. 输入分析对象  
3. 生成报告  

---

## 文件保存位置（本地）

- 上传资料：`knowledge_base/raw_uploads/`
- 生成报告：`outputs/`
- 向量库：`knowledge_base/chroma_db/`
